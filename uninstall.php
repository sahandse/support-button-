<?php
if ( ! defined( 'WP_UNINSTALL_PLUGIN' ) ) exit;
// Data is intentionally retained by default to avoid accidental loss.
// Site owners can remove options/tables manually or through a future cleanup toggle.
